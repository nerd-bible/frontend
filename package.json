{
	"name": "@nerd-bible/frontend",
	"type": "module",
	"workspaces": [
		"native",
		"core",
		"valio",
		"web"
	],
	"scripts": {
		"start": "npm run start --workspace='@nerd-bible/web'",
		"build": "npm run build --workspaces",
		"fmt": "prettier -l --ignore-path .gitignore .",
		"fmt-fix": "prettier --fix --ignore-path .gitignore ."
	},
	"devDependencies": {
		"prettier": "^3.8.0",
		"vitest": "^4.0.18"
	}
}
